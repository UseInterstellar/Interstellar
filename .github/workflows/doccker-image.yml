name: Publish image to Docker Hub

on:
  [workflow_dispatch]

jobs:
  publish_image:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: build
        run: |
          docker build . -t ${{ secrets.peely16756387}}/${{ secrets.peely}}:latest
      - name: publish
        run: |
          docker login -u ${{ secrets.peely16756387}} -p ${{ secrets.dckr_pat_Pxo8V1MqWdTHG64gyDBbklVyJ7U}}
          docker push ${{ secrets.peely16756387}}/${{ secrets.peely}}:latest
